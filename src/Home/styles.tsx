import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    marginTop: 20,
  },
  alert: {
    width: 150,
    marginBottom: 10,
  },
  title: {
    paddingTop: 20,
    paddingBottom: 20,
  },
});
